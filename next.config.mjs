/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  output: 'export',
  basePath: '/home',
  assetPrefix: '/home/',
  reactCompiler: true,
  devIndicators: false
};

export default nextConfig;
